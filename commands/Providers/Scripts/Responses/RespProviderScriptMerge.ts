
	/**
	 * A container for the {@link providerScript}.
	 */
	export class RespProviderScriptMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		providerScript: RespIdCompany;
	}