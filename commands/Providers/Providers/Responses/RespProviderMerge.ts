
	/// <summary>
	/// A container for the <see cref="provider"/>.
	/// </summary>
	export class RespProviderMerge extends Response {
		/// <summary>
		/// An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		/// </summary>
		public provider: RespIdCompany;
	}