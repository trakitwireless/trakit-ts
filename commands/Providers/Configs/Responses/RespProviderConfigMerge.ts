
	/**
	 * A container for the {@link providerConfig}.
	 */
	export class RespProviderConfigMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		providerConfig: RespIdCompany;
	}