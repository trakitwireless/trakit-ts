
	/**
	 * A container for the <see cref="formResult"/>.
	 */
	export class RespFormResultMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		formResult: RespIdCompany;
	}