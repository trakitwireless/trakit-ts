
	/**
	 * A container for the <see cref="behaviour"/>.
	 */
	export class RespBehaviourMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		behaviour: RespIdCompany;
	}