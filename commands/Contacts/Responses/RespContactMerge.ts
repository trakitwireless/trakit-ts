
	/**
	 * A container for the {@link contact}.
	 */
	export class RespContactMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		contact: RespIdCompany;
	}