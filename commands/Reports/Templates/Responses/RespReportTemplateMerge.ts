
	/**
	 * A container for the {@link reportTemplate}.
	 */
	export class RespReportTemplateMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		reportTemplate: RespIdCompany;
	}