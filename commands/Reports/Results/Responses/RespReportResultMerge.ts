
	/**
	 * A container for the {@link reportResult}.
	 */
	export class RespReportResultMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		reportResult: RespIdCompany;
	}