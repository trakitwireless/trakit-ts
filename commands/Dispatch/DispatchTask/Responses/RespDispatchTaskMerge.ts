
	/**
	 * A container for the <see cref="dispatchTask"/>.
	 */
	export class RespDispatchTaskMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		dispatchTask: RespIdCompany;
	}