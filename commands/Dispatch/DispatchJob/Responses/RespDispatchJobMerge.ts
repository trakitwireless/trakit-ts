
	/**
	 * A container for the {@link dispatchJob}.
	 */
	export class RespDispatchJobMerge extends Response {
		/**
		 * An object which contains the <c>id</c> and <c>company</c> keys when there is no error.
		 */
		dispatchJob: RespIdCompany;
	}